# v0.2.0 release prep — working notes & checklist

- **Date:** 2026-06-25
- **Project:** camdl
- **Tags:** release, v0.2.0, working-notes
- **Status: TEMPORARY** — scratch coordination doc for cutting v0.2.0. Delete
  once the release ships. Not a normative doc; the normative ones are
  `VERSIONING.md` (policy) and `RELEASING.md` (runbook).

## Context

v0.2.0 is the **first published release** of camdl. The `v0.1.0-alpha` tag
(2026-05-15) exists but was never published as a GitHub Release, so the notes
fold in everything since alpha (`v0.1.0-alpha..HEAD`, ~818 commits) and read as
"here is camdl 0.2.0," not a thin delta.

Cut the version **explicitly** as `0.2.0` — the bare `-alpha` tag has no dotted
counter, so `git-cliff --bumped-version` continues it to `v0.1.0-alpha.1`, which
is wrong (see `RELEASING.md`).

## Decisions made (this session)

- **Version: `0.2.0`** (MINOR — feats + breaking changes since alpha).
- **`fit predict`: promote to a Highlight** ("posterior-predictive checks and
  forecasts"; wording stays modest — only the FreeForward × Posterior slice
  shipped).
- **PMMH: classify as production** (maintainer call). See the alignment task
  below — this is more than a release-notes relabel.
- **`camdl if2` stub: keep.** It is an _intentional_ deprecation stub (gh#147
  M3.3) that prints a migration message → `camdl fit run` and exits 2 — the
  "signpost the migration" pattern, not dead code. (Earlier mischaracterized as
  a dead leftover; corrected. Retiring the stub → bare clap error is a
  post-alpha call, not for 0.2.0.)

## Master checklist (before `make release-publish VERSION=0.2.0`)

- [x] `check-update` command shipped (`0b6278af`, CI green, installed).
- [x] `CLAUDE.md` "Releasing" pointer added (discoverability for agents + future
      self).
- [x] `scripts/release.sh suggest` backup-tag bug fixed
      (`git describe --match
      'v[0-9]*'` so it anchors on `v0.1.0-alpha`,
      not the `cas-overhaul` backup tag).
- [x] `RELEASE_NOTES-0.2.0.md` first full draft over the correct range (replaces
      the stale June-8 doc-hardening slice).
- [x] **Pre-release light review** (3 agents) + **Tier 1 applied** (README
      rewrite, intro/user-features/spec stale examples, `if2` hidden) — landed,
      CI green.
- [~] **`camdl dev` move + `check` linter** — IN FLIGHT (`make test` running).
      `compile`/`doctest`/`eval`/`reindex` → `camdl dev`; `check` now reports
      diagnostics only (no summary). Then: 2 commits + release-notes +
      language-changes entries.
- [ ] **Workflow doc** — v2 written/rendered/reviewed
      (`2026-06-26-fitting-workflow-v2.md`); PENDING maintainer read → land as
      `docs/workflow.md` (absorbs Tier 2: inference.md fit.toml + IF2 framing).
- [ ] **PMMH → production relabel** (decided: production; PGAS stays default).
      Drop "experimental" + reframe the `T>500` warning as "prefer PGAS for long
      series" in: the `fit methods` registry string, `fit-toml.md`,
      `inference.md`, `CLAUDE.md`.
- [ ] **README finalize** — maintainer prose pass + wire in the math↔DSL figure
      (commit `blog-index-color.png` + `blog_index_color_figure.py` to
      `docs/assets/`+`scripts/`).
- [ ] **`docs/language-changes.md`** current for every breaking change since
      alpha (obs-block reshape, `--params`→`--init`, the `camdl dev` move, the
      `check` output change, …).
- [ ] **`RELEASE_NOTES-0.2.0.md` final pass** — fold in the dev move + check
      linter; resolve editorial calls (lead order; `mh on ode`; reactive
      interventions mention).
- [ ] Gate: `make test` green + CI green + goldens clean on the release commit.
- [ ] Cut it: `make release-prep VERSION=0.2.0` → refresh notes via
      `/release-notes 0.2.0` → `make release-publish VERSION=0.2.0`.
- [ ] (No README status-badge change — staying alpha, not crossing to beta.)
- [ ] After ship: delete this working note.

Nice-to-have, not blockers: the review minors (`.camdl`-not-found OCaml
exception → clean diagnostic; `mre simulate --help` examples; `--align-zero`
wording); a committed worked example under `examples/`.

## Workflow doc reorientation (the next big task — WAIT for incoming notes)

**Problem (my understanding):** the current fitting-workflow documentation is
too **IF2-centric** and organized around the **staged-fit** mechanics. That
framing teaches the pipeline plumbing rather than the actual task — how to fit a
model and know whether the fit is any good.

**Desired reorientation:** a general "how to fit" guide that orients the reader
to the _task_, not one algorithm's stages:

- The menu of fitting subcommands and when to reach for each (`fit run`,
  `pfilter`, `profile`, `survey`) — what question each answers.
- Generating **synthetic data** to validate recovery before trusting a real fit
  (simulate from known params → fit → did you get them back?).
- Reading **particle-filter health**: ESS and the `--pf-health` (ESS / τ²)
  diagnostic; recognizing **particle degeneracy** (the filter collapsing to a
  few distinct ancestors).
- Diagnosing **failing fits**: stuck chains, poor mixing, low ESS — what to look
  at and what each symptom usually means.
- The **PGAS-specific failure mode** (the one worth spelling out): PGAS
  alternates a conditional SMC (CSMC) that resamples the latent trajectory given
  a reference path, and a parameter update conditioned on that trajectory. When
  the particle filter degenerates, the conditional sampler **collapses back onto
  the reference path** — the latent trajectory isn't actually
  refreshed/explored, so the parameter moves (conditioned on a non-mixing latent
  state) stall. Symptom: the chain looks stuck / parameters barely move / high
  autocorrelation, and the root cause is _the conditioned sampler isn't
  exploring the latent space_, not the parameter proposal per se. Mitigations to
  mention: more particles, check the model isn't near-deterministic (degenerate
  observation density), reparameterize, check ancestor sampling is actually
  refreshing the path.

**Do not draft this verbatim from the dictation** — the above is the _intent_,
to be written cleanly once the incoming agent notes on recent fitting workflows
arrive. Identify the actual file first (likely the `camdl docs workflow` topic
source under `docs/`; confirm the path).

## Pre-release review findings (from the review subagents)

**Tier 1 APPLIED (2026-06-25):** README rewritten (leads with the model +
commands, engineering as capability); `if2` hidden from the command list (kept
as a migration stub); `intro.md` false "inference not implemented" limitation
removed; `user-features.md` `periodic 'ratio` ×2 + false `--obs-model` claim
fixed; `camdl-language-spec.md` `camdl if2` standalone teaching removed. All
snippets verified vs. the binary; doctests 0 FAIL; build clean. **Tier 2**
(inference.md fit.toml + IF2 framing) folds into the workflow-doc rewrite below.

### CLI surface / help text (agent 1 — done)

Embarrassing:

- **`if2` removed-but-still-advertised.** `camdl --help` lists
  `if2  Standalone iterated filtering (IF2 / MIF2)` and `camdl if2 --help`
  prints a normal help block (exit 0) — only _running_ it hits "has been
  removed." A removed command shouldn't be advertised as live. Fix:
  `#[command(hide = true)]` on the `If2` variant (keeps the migration stub
  working but unlists it) + honest description; alias `mif2` too. → this is the
  real `if2` cleanup.
- **`camdl docs language` topic still teaches `camdl if2 MODEL --data …`** as a
  working standalone subcommand (ships in the binary). Cut to the fit.toml
  one-stage form it already describes right after.

Minor:

- **`.camdl` not-found leaks a raw OCaml exception:**
  `error: Fatal error: exception Sys_error("…: No such file or directory")` —
  vs. the clean Rust-side `.ir.json` error. Wrap the camdlc-side file-not-found
  in a normal diagnostic (error quality is a feature).
- **`mre simulate --help` examples are copy-pasted from `simulate`** (say
  `camdl simulate …`, never show `-b/--bundle`). Give it real
  `camdl mre simulate … -b out.mre.tar.gz` examples.

Nits: `--draws posterior.tsv` example reads ambiguously next to the
`"posterior"` keyword mode (rename the example file);
`lineage cohort --align-zero` help says "(default)" on a bare bool flag
(contradictory wording).

Clean: no Rust panics on bad input (all bad invocations give clean diagnostics +
sane exit codes); no TODO/placeholder in help or embedded docs; no other
removed-feature references outside the changelog; `docs` list + `check-update`
clean.

Confirms the version-bump step: binary is `0.1.0` —
`make release-prep
VERSION=0.2.0` bumps the manifests before the tag (already in
the checklist).

### Code embarrassments (agent 2 — done)

**Clean.** No leftover `dbg!`/debug prints, no `todo!`/`unimplemented!`, no
hardcoded abs paths in production, no dead commented-out code. The three
plausible user-input panics were each chased to ground and found **safe**:
`fit/runner.rs:575` sort-`expect` (non-finite `condition_from` is caught
upstream in `resolve_condition_from` — verified by reproduction); `main.rs:2492`
prior sampling `unwrap` (OCaml `validate_prior_values` rejects bad
shape/alpha/beta at compile time, E277); `pgas.rs:870` tempering index (internal
invariant). The ~917 other `unwrap`/`expect` are tests or guarded invariants.

Nits (optional, not blockers):

- **`pfilter --trace - | head` can SIGPIPE-panic** (`writeln!(...).unwrap()` on
  `BufWriter<Stdout>`) — the standard Rust-CLI broken-pipe issue. Low severity.
- **`fit/trace_writer.rs:42,47` `panic!` on file-open** vs `pfilter.rs:663`'s
  graceful `eprintln!`+`exit(1)` — not user-reachable (CAS-internal dir), but
  unify on the graceful path for tidiness.
- Known/deferred: `pfilter.rs:957` `// TODO: make NA token a user option`.

### Docs / examples / install drift (agent 3 — done)

Most rot is in **README.md** + a few user docs; DSL snippets in the tutorials
mostly compile (0 doctest fails across 9 docs). All verified vs. the live
binary.

EMBARRASSING (a new user copying these hits an error or is misled):

- **README.md broken examples:** L239 `sinusoidal {` needs `sinusoidal 'ratio {`
  (E001); L258-259 old `every`/`likelihood` obs syntax → new `columns{}` + `~` +
  `emit_schedule`; L326-340 `camdl if2 …` shown as working (it's the removed
  stub) → fit.toml `algorithm="if2"` stage; L345-348
  `camdl profile --focal R0
  --grid …` → `--sweep "R0=…"` (the
  `--focal`/`--grid` flags don't exist); L363 `camdl fit status` is not a
  subcommand. **L301 wrong default backend** — says Gillespie default; actual
  default is `chain_binomial`.
- **docs/intro.md L546-547** "Known limitations" falsely says observation/
  inference is _not yet implemented_ — the entire v0.2 inference stack ships + 8
  goldens carry obs blocks. Delete the limitation.
- **docs/inference.md** non-parsing fit.toml examples (deny_unknown_fields):
  `init = "from_mle"` as a bare string ~8× (it's a struct variant — use
  `init_mle = "<stage>"` and omit `init`; init.rs:121); `[pgas]` →
  `[stages.pgas]`; top-level `[holdout]`/bare `[data]` keys →
  `[data.observations]`/`[data.holdout]` (also fit-toml.md:55-56);
  `max_treedepth` → `max_tree_depth`.
- **docs/user-features.md** L132/L446 `periodic {` needs `periodic 'ratio {`
  (E001 — the marquee examples); L362-363 false claim of
  `--obs-model
  discretized_normal` (no such flag/family). Delete.
- **examples/** holds only untracked scratch (`seir_age_sobol/output/`,
  gitignored) — no committed worked example. Delete the scratch or add a real
  one.

MINOR: README L561 lists removed "tau-leap" backend; README L580-581 "schema
stale vs v0.4 (C8)" — IR is 0.19, stale refs; docs/commands.md L46 lists
non-existent `fit status`/`fit where`; docs/inference.md L430 old obs syntax in
prose; dead archive links (proposals moved to `docs/dev/proposals/archive/…`:
dsl-cheatsheet:14, dates:28/52/270/408, inference:496/803); dates.md L218
`--time-col` is `data split`-only.

NIT: user-features.md L355-359 stale `--trace` columns; agents.md L192 / schema
notes hardcode `ir_version "0.4"`; runtimes.md L372 missing `simulate` verb;
commands.md L94 `inspect --cost-report` not in `--help`.

CLEAN: install.sh; `--starts-from`/`summary{}`/`scope=` correctly absent;
badge + links resolve; concepts/mre/dates/agents DSL snippets compile.

NOTE the overlap: the inference.md / workflow.md fit.toml + IF2-framing fixes
belong with the **workflow-doc reorientation** (below), not a separate pass.

## Open editorial questions for the release notes (maintainer's call)

1. **Lead order** of Highlights — currently inference-first; swap to DSL-first
   (authoring experience) if preferred.
2. **`mh on ode`** ("Phase 1") — advertise under Inference, or cut for 0.2.0?
3. **Reactive interventions** (parsed/validated but runtime-rejected outside
   forward chain-binomial) — mention at all in a first release, or hold?
4. **Calendar-time** one-liner — gut-check it's accurate to what a user can do
   end-to-end today.

## Release-cut procedure (reminder)

```
make release-suggest                 # commits since v0.1.0-alpha + suggested bump
make release-prep VERSION=0.2.0      # bump every manifest + regenerate CHANGELOG.md
#   → refresh RELEASE_NOTES-0.2.0.md via the /release-notes 0.2.0 skill, edit
make test                            # + confirm CI green, goldens clean
make release-publish VERSION=0.2.0   # commit + annotated tag + push + gh release (prompts; IRREVERSIBLE)
```
