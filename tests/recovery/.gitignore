# Generated by `make`: synthetic observation streams and CAS run output. Both
# are regenerable from the committed recipe (model.camdl + truth.toml + the fit
# configs), so the bytes are not committed. (`results/` is also caught
# repo-wide; listed here for local clarity.)
**/data/
**/results/
